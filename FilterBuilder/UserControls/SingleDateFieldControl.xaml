<UserControl x:Class="CmcScriptNet.FilterBuilder.UserControls.SingleDateFieldControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <StackPanel Orientation="Vertical">
        <Label Content="Field Value:"/>
        <!-- we can later change these to proper numberpicker controls-->
        <DatePicker Text="{Binding FieldValue, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"/>
    </StackPanel>
</UserControl>
